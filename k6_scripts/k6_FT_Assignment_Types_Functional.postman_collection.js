// Auto-generated by the postman-to-k6 converter

import "./libs/shim/core.js";
import "./libs/shim/expect.js";
import "./libs/shim/urijs.js";
import { group } from "k6";

export let options = { maxRedirects: 4 };

const file = (() => {
  // Load data file
  const text = open("./data.json");
  const rows = JSON.parse(text);
  return rows;
})();
options.iterations = file.length;

const Iteration = Symbol.for("iteration");
const Request = Symbol.for("request");
postman[Symbol.for("initial")]({
  options,
  environment: {
    envWebHost: "sisclientweb-900162.campusnexus.dev",
    envAuthUser: "Administrator",
    envAuthPassword: "testing",
    envWebScheme: "https",
    envCollection:
      "/api/faculty-workload-hr-integration/job-functions-collection"
  },
  data: file
});

export default function() {
  postman[Iteration]();

  group("New Folder", function() {
    postman[Request]({
      name: "Initialize",
      id: "dfca948c-b1a2-453c-a57f-6901fb57fcb6",
      method: "GET",
      address: "{{gApiUrl}}",
      headers: {
        "Content-Type": "Content-Type"
      },
      pre() {
        // This is the first request in the collection, clear the global variables
        // used in the CRUD collection.   Do not do a clear all globals using pm.globals.clear
        // because that will remove the common code global variable gCmcUtility.

        pm.globals.unset("gApiUtility");
        pm.globals.unset("gApiUrl");
        pm.globals.unset("gAuthHeaderKey");
        pm.globals.unset("gAuthHeaderValue");
        pm.globals.unset("gUrl");
        pm.globals.unset("gCreateRequestBody");
        pm.globals.unset("gSaveRequestBody");
        pm.globals.unset("gGetRequestBody");
        pm.globals.unset("gObject");
        pm.globals.unset("gIndex");
        pm.globals.set("gIndex", 0);

        var apiUtilitHost = "";

        if (!pm.environment.has("envApiUtilityHost")) {
          apiUtilityHost = "10.156.14.135:9000";
        } else {
          apiUtilityHost = pm.environment.get("envApiUtilityHost");
        }

        var requestUrl =
          "http://" + apiUtilityHost + "/automation/apiUtility.js";

        pm.globals.set("gApiUrl", requestUrl);

        //console.log(pm.globals.get("gUrl"));
      },
      post(response) {
        pm.globals.set("gApiUtility", responseBody);

        try {
          eval(pm.globals.get("gApiUtility"));
        } catch (e) {}

        pm.test("Framework ready", function() {
          pm.expect(typeof apiUtility).to.be.equal("object");
        });

        pm.test("Verify response status is " + 200, function() {
          pm.expect(pm.response.code).to.equal(200);
        });

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "Get Campus ID",
      id: "2a7237db-0c69-44a2-85ab-df91ee87f53e",
      method: "GET",
      address: "{{gUrl}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        // Make authorization header using basic auth or API key settings in the data file.
        // If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        //console.log(pm.globals.get('gAuthHeaderKey'));
        //console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        // Make request body.
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gCreateRequestBody", JSON.stringify(req));
        //console.log(pm.globals.get('gCreateRequestBody'));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });
        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "Create",
      id: "d0963ad3-b259-432d-8485-9140e5975e4a",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gCreateRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        // Make authorization header using basic auth or API key settings in the data file.
        // If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        //console.log(pm.globals.get('gAuthHeaderKey'));
        //console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        // Make request body.
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gCreateRequestBody", JSON.stringify(req));
        //console.log(pm.globals.get('gCreateRequestBody'));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });
        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "SaveNew Overload Assignment",
      id: "670fc212-a642-42e5-910e-3a1e657a70c5",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gSaveRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        //Make authorization header using basic auth or API key settings in the data file.
        //If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        // console.log(pm.globals.get('gAuthHeaderKey'));
        // console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        // Make request body.
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gSaveRequestBody", JSON.stringify(req));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });
        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "SaveNew onload Assignment type linked to overload assignment type",
      id: "63e74c52-fd4d-464e-aff4-e11076d75f32",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gSaveRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        //Make authorization header using basic auth or API key settings in the data file.
        //If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        // console.log(pm.globals.get('gAuthHeaderKey'));
        // console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        // Make request body.
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gSaveRequestBody", JSON.stringify(req));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });
        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "Get After SaveNew onload Assignment type",
      id: "3ad79931-0f89-493c-98dd-eaf88c907004",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gGetRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));
        let API = data.Apis[pm.globals.get("gIndex")];
        //Make authorization header using basic auth or API key settings in the data file.
        //If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        // console.log(pm.globals.get('gAuthHeaderKey'));
        // console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        // Make request body.
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gGetRequestBody", JSON.stringify(req));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });

        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "Save onload assignment type",
      id: "2fd14a63-e8fe-4b67-b699-153cf2c690ff",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gSaveRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        // Make authorization header using basic auth or API key settings in the data file.
        // If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        //console.log(pm.globals.get('gAuthHeaderKey'));
        //console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        // Make request body.
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gSaveRequestBody", JSON.stringify(req));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });

        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "Create Load Validation Policy",
      id: "d82a19a9-14be-4f2a-a9be-8760e30f5ffe",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gCreateRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        // Make authorization header using basic auth or API key settings in the data file.
        // If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        //console.log(pm.globals.get('gAuthHeaderKey'));
        //console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        // Make request body.
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gCreateRequestBody", JSON.stringify(req));
        //console.log(pm.globals.get('gCreateRequestBody'));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });
        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "SaveNew Load Validation Policy",
      id: "9ff082cd-f553-4a18-b514-59f12d84d0f3",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gSaveRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        //Make authorization header using basic auth or API key settings in the data file.
        //If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        // console.log(pm.globals.get('gAuthHeaderKey'));
        // console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        // Make request body.
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gSaveRequestBody", JSON.stringify(req));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });
        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "Get  onload assignment type",
      id: "8c538cd6-5cf4-4cc6-95be-baf684b05aa6",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gGetRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        //Make authorization header using basic auth or API key settings in the data file.
        //If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        // console.log(pm.globals.get('gAuthHeaderKey'));
        // console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        // Make request body.
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gGetRequestBody", JSON.stringify(req));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });

        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "Delete Load Rule",
      id: "941639b5-8831-4840-b3d8-40d698887eb0",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gDeleteRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        //Make authorization header using basic auth or API key settings in the data file.
        //If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        // console.log(pm.globals.get('gAuthHeaderKey'));
        // console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        //Make request
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gDeleteRequestBody", JSON.stringify(req));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });

        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "Get Load Rule",
      id: "23977222-dc90-406c-a5fe-c95e659b2ce1",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gGetRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        //Make authorization header using basic auth or API key settings in the data file.
        //If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        // console.log(pm.globals.get('gAuthHeaderKey'));
        // console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        // Make request body.
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gGetRequestBody", JSON.stringify(req));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });

        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "Delete onload assignment type association check",
      id: "98c23655-2e45-43d0-9f0f-d6a2c12e2f29",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gDeleteRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        //Make authorization header using basic auth or API key settings in the data file.
        //If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        // console.log(pm.globals.get('gAuthHeaderKey'));
        // console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        //Make request
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gDeleteRequestBody", JSON.stringify(req));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });

        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "Get overload assignment type",
      id: "83e81eb0-a786-4a69-a890-e1101b23bd3f",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gGetRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        //Make authorization header using basic auth or API key settings in the data file.
        //If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        // console.log(pm.globals.get('gAuthHeaderKey'));
        // console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        // Make request body.
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gGetRequestBody", JSON.stringify(req));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });

        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "Delete overload assignment type Cross Validation",
      id: "3993f033-6979-4cf5-a272-1333e66bb437",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gDeleteRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        //Make authorization header using basic auth or API key settings in the data file.
        //If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        // console.log(pm.globals.get('gAuthHeaderKey'));
        // console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        //Make request
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gDeleteRequestBody", JSON.stringify(req));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });

        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "Get After Save onload assignment type",
      id: "9c40af6a-49b8-4a36-a4be-f406f5754c45",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gGetRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        //Make authorization header using basic auth or API key settings in the data file.
        //If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        // console.log(pm.globals.get('gAuthHeaderKey'));
        // console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        // Make request body.
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gGetRequestBody", JSON.stringify(req));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });

        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "Delete onload assignment type",
      id: "bcc57d7c-05c0-414c-947b-aab42e716a28",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gDeleteRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        //Make authorization header using basic auth or API key settings in the data file.
        //If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        // console.log(pm.globals.get('gAuthHeaderKey'));
        // console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        //Make request
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gDeleteRequestBody", JSON.stringify(req));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });

        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "Get After Delete onload assignment type",
      id: "c2dd3da8-4234-40ba-a630-35466b01a8a1",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gGetRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        //Make authorization header using basic auth or API key settings in the data file.
        //If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        // console.log(pm.globals.get('gAuthHeaderKey'));
        // console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        // Make request body.
        // Make request body.
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gGetRequestBody", JSON.stringify(req));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });

        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "Delete overload assignment type",
      id: "70377e2f-57c9-4c5a-bfb5-2a5c735dc556",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gDeleteRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        //Make authorization header using basic auth or API key settings in the data file.
        //If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        // console.log(pm.globals.get('gAuthHeaderKey'));
        // console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        //Make request
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gDeleteRequestBody", JSON.stringify(req));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });

        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });

    postman[Request]({
      name: "Get After Delete overload assignment type",
      id: "337afec1-2023-4878-a402-3edc85391f37",
      method: "POST",
      address: "{{gUrl}}",
      data: "{{gGetRequestBody}}",
      headers: {
        "Content-Type": "application/json",
        [`${pm[Var]("gAuthHeaderKey")}`]: "{{gAuthHeaderValue}}"
      },
      pre() {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        let API = data.Apis[pm.globals.get("gIndex")];

        //Make authorization header using basic auth or API key settings in the data file.
        //If data file does not autornization info, use environment variable if available.
        var hdr = apiUtility.makeAuthorization(
          API.ApiKey,
          API.User,
          API.Password,
          pm.environment.get("envApiKey"),
          pm.environment.get("envAuthUser"),
          pm.environment.get("envAuthPassword")
        );
        pm.globals.set("gAuthHeaderKey", hdr.key);
        pm.globals.set("gAuthHeaderValue", hdr.value);
        // console.log(pm.globals.get('gAuthHeaderKey'));
        // console.log(pm.globals.get('gAuthHeaderValue'));

        // Make URL
        pm.globals.set(
          "gUrl",
          apiUtility.makeUrl(
            pm.environment.get("envWebScheme"),
            pm.environment.get("envWebHost"),
            API.RequestUrl
          )
        );
        //console.log(pm.globals.get('gUrl'));

        // Make request body.
        // Make request body.
        var request = {};
        apiUtility.setChainProps(API.Chain, request);
        var req = apiUtility.mergeRequestBody(request, API.RequestBody);

        pm.globals.set("gGetRequestBody", JSON.stringify(req));
      },
      post(response) {
        // Load API utility code
        eval(postman.getGlobalVariable("gApiUtility"));

        // Get API configuration from data file
        let API = data.Apis[pm.globals.get("gIndex")];

        // --- Start test verifications             ---
        // --- Add your custom tests in 'test verifications' block ---
        pm.test("Verify response status is " + API.ResponseStatus, function() {
          pm.expect(pm.response.code).to.equal(API.ResponseStatus);
        });

        pm.test("Verify response body matches expected response ", function() {
          var actualresponse = JSON.parse(responseBody);
          pm.expect(
            apiUtility.verifyResponse(API.ResponseBody, pm.response.json())
          ).to.be.true;
        });
        // --- End test verification ---

        // If status is OK save properties for chaining
        if (pm.response.code === 200) {
          var rb = pm.response.json();
          if (API.ChainSource.length > 0) {
            let str = API.RequestId + ".response";
            apiUtility.globalStoreWrite(str, rb);
          }
        }

        // Increment the API index
        pm.globals.set("gIndex", Number(pm.globals.get("gIndex")) + 1);

        console.log(
          "--------------------------------------------------------------------------------------"
        );
      }
    });
  });
}
